agent_id: "{{agent_id}}"
name: "{{agent_name}}"
role: "{{agent_role}}"
version: "1.0.0"
capabilities:
  - pytest_runner
  - obsidian_vault_parser
  - task_forest_engine
allowed_skills:
  - dnk-task-graph-manager
  - obsidian-task-forest
memory_dir: "core/agent_factory/agents/{{agent_id}}/memory/"
