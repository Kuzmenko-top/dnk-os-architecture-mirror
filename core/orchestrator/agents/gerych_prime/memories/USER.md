Maxim enforces strict verification standards: reject narrative claims of fixes without exact code/diff proof; require line-by-line GitHub file diff verification; maintain zero-action / containment stance on security issues until verified artifacts arrive.
§
User prefers important responses, architectural designs, and briefings to also be created/saved as structured .md files with high-quality formatting for human readability.
§
Obsidian vault path for DNK_HUB notes: access via relative path `./docs/notes/` or virtual prefix `vault:<note.md>` (points to `~/Documents/DNK_HUB My Notes/DNK_HUB My Notes`). Notes require YAML frontmatter, MRH header, and rich [[wikilinks]] cross-linking. Never use absolute paths!
§
Proactively propose saving key architectural decisions, algorithms, and rationale to the Obsidian vault after each significant task to ensure future maintainability and effortless modernization.
§
Maxim evaluates features by founder-level operational leverage (cognitive clarity, bottleneck elimination, speed). When finishing slices, provide immediate actionable local testing recipes: exact URLs, step-by-step UI verification cues, and CLI inspection commands.